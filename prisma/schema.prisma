// prisma/schema.prisma

// 🔹 Configuração do banco
datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

// 🔹 Prisma Client
generator client {
  provider = "prisma-client-js"
}

// 🔹 Model de Livro
model Book {
  id          Int      @id @default(autoincrement())
  title       String
  author      String
  year        Int
  pages       Int
  currentPage Int
  status      String
  isbn        String?
  cover       String
  rating      Float
  synopsis    String
  notes       String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  // Relacionamento Many-to-Many com Genre
  genres      Genre[]
}

// 🔹 Model de Gênero
model Genre {
  id    Int    @id @default(autoincrement())
  name  String @unique

  // Relacionamento Many-to-Many com Book
  books Book[]
}
